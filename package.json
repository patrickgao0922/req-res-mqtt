{
  "name": "req-res-mqtt",
  "version": "1.0.0",
  "description": "MQTT Library with response",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon --exec babel-node src/index.js",
    "prestart": "babel src --out-dir dist",
    // "start": "node dist/index.js",
    // "stop": "forever stop igloo-node-rest-api",
    "test": "mocha --compilers js:babel-core/register --timeout 80000"
  },
  "keywords": [
    "mqtt"
  ],
  "author": "Patrick Gao <patrickgao0922@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "mqtt": "^2.14.0"
  },
  "devDependencies": {
    "nodemon": "^1.12.1"
  }
}
